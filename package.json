{
  "name": "olympus-stake",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ olympus-stake",
    "create-local": "graph create --node http://localhost:8020/ olympus-stake",
    "remove-local": "graph remove --node http://localhost:8020/ olympus-stake",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 olympus-stake"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1"
  },
  "devDependencies": {
    "@types/node": "^16.10.1"
  }
}
